import ProjectList from "../ProjectList";

const Portfolio = () => {
    return (
        <section className='box'>
            <h2 className='title'>Click on any of the projects below to check a bit of my past work!</h2>
           <ProjectList /> 
        </section>
    )
}

export default Portfolio;